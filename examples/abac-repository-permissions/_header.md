# ABAC repository permissions example

This deploys the Container Registry module with attribute-based access control (ABAC) enabled by setting `role_assignment_mode` to `AbacRepositoryPermissions`, allowing access to be scoped per repository as recommended by Microsoft for new deployments.
